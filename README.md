# Temple Aviv Judea

Astro website for Temple Aviv Judea, a Messianic Jewish congregation in Fullerton, California.

## Local development

```sh
pnpm install --frozen-lockfile
pnpm run dev
```

Use Node 24, which is pinned in `.node-version`. Install pnpm 11 if it is not already available; the repository pins the exact pnpm version in `package.json`. Its architecture settings install the native and WASM packages required by Astro's local and Cloudflare renderers.

To run the local TinaCMS editor without a TinaCloud account:

```sh
pnpm run dev:cms
```

Open the URL printed by Tina, then visit `/admin/`. Local CMS mode reads and writes the JSON content files in this repository.

## Verification

```sh
pnpm run validate:content
pnpm run check
pnpm run build
pnpm run build:cms:local
```

## Deployment

Pushes to `main` deploy automatically through GitHub Actions to:

`https://skarath13.github.io/temple-aviv-judea/`

The production `avivjudea.org` domain is intentionally not connected. Its current website and DNS remain unchanged until a separate cutover is approved.

The default build remains compatible with the current GitHub Pages base path. CMS mode switches Astro to the Cloudflare adapter and a root base path.

## TinaCMS content model

The selected CMS is TinaCMS, with TinaCloud planned as the hosted authentication, content API, and Git publishing layer.

- Site identity, contact details, service times, links, and navigation: `src/content/settings/site.json`
- Every public page's SEO, hero, designed body content, images, accessible descriptions, links, and optional page-builder sections: `src/content/pages/*.json`
- Up to three homepage events: `src/content/events/events.json`
- Tina schema and editor controls: `tina/`
- Media uploaded through Tina: `public/uploads/`

The schema provides a page-specific, no-code editor for every existing designed section while keeping the route structure, styling, payment mechanics, and component templates developer-controlled. The Cloudflare-mode build also wires Tina's Astro island route and click-to-edit field markers for visual page editing.

The public GitHub Pages build uses `/admin-preview/` for the nonfunctional CMS explanation. In Tina and Cloudflare mode, `/admin/` is reserved for the real editor generated by Tina.

## TinaCloud and Cloudflare Workers activation

Account setup is intentionally deferred. When ready:

1. Create a TinaCloud project and connect this repository.
2. Add `PUBLIC_TINA_CLIENT_ID` and `TINA_TOKEN` as encrypted Cloudflare build variables. Do not commit either value.
3. Set `SITE_URL` to the final HTTPS origin.
4. Configure the Worker build command as `pnpm run build:cloudflare` and deploy command as `pnpm exec wrangler deploy`.
5. Keep `WORKERS_CI_BRANCH` available in Cloudflare builds so Tina writes to the matching Git branch.
6. After the first deployment, create and pin the `SESSION` KV namespace binding required by Tina's Cloudflare integration.

The local variable names are documented in `.env.example`. `wrangler.jsonc` already enables `nodejs_compat`, but it intentionally contains no account IDs, secrets, routes, or production bindings.

Tina content is Git-backed. Cloudflare D1 and R2 are not required for ordinary page editing. Reserve those services for future runtime application data that does not belong in versioned site content.

The home hero uses separate desktop and mobile artwork through a responsive `<picture>` element:

- `public/images/hero-desktop.jpg`
- `public/images/hero-mobile.jpg`

See `ASSET_SOURCES.md` for the live-site asset inventory and local migration map.
